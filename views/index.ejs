<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Meeting Notes Summarizer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .alert {
            padding: 1rem;
            margin-bottom: 1.5rem;
            border-radius: 0.5rem;
            border-width: 1px;
        }
        .alert-success {
            background-color: #d1fae5;
            border-color: #6ee7b7;
            color: #065f46;
        }
        .alert-error {
            background-color: #fee2e2;
            border-color: #fca5a5;
            color: #991b1b;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800">

    <div class="container mx-auto p-4 md:p-8 max-w-4xl">
        <header class="text-center mb-8">
            <h1 class="text-4xl font-bold text-gray-900">AI Meeting Notes Summarizer</h1>
            <p class="text-lg text-gray-600 mt-2">Transform meeting notes into clean, structured summaries.</p>
        </header>

        <main class="bg-white p-6 md:p-8 rounded-2xl shadow-lg">

            <% if (successMessage) { %>
                <div class="alert alert-success" role="alert"><%= successMessage %></div>
            <% } %>
            <% if (errorMessage) { %>
                <div class="alert alert-error" role="alert"><%= errorMessage %></div>
            <% } %>

            <form action="/summarize" method="POST" enctype="multipart/form-data" class="space-y-6">
                <!-- Step 1: Upload or Paste -->
                <section>
                    <h2 class="text-2xl font-semibold mb-4 border-b pb-3">1. Provide Transcript</h2>
                    <div>
                        <label for="transcript" class="block text-sm font-medium text-gray-700 mb-1">Upload Transcript (.txt file)</label>
                        <input type="file" name="transcript" id="transcript"
                               class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100 cursor-pointer">
                    </div>
                    <div class="text-center my-2 text-gray-500 text-sm">OR</div>
                    <div>
                        <label for="transcript_text" class="block text-sm font-medium text-gray-700 mb-1">Paste Transcript Text</label>
                        <textarea name="transcript_text" id="transcript_text" rows="8"
                                  placeholder="Paste your meeting transcript here..."
                                  class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm"></textarea>
                    </div>
                </section>

                <!-- Step 2: Customize -->
                <section>
                    <h2 class="text-2xl font-semibold mb-4 border-b pb-3">2. Customize Summary</h2>
                     <div>
                        <label for="prompt" class="block text-sm font-medium text-gray-700 mb-1">Custom Instruction (Optional)</label>
                        <input type="text" name="prompt" id="prompt"
                               placeholder="e.g., 'Summarize in bullet points for executives'"
                               value="<%= customPrompt %>"
                               class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                    </div>
                </section>

                <button type="submit"
                        class="w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors">
                    Generate Summary
                </button>
            </form>

            <% if (summary) { %>
            <!-- Summary and Share Section -->
            <section id="share-section" class="mt-10">
                <h2 class="text-2xl font-semibold mb-4 border-b pb-3">3. Review & Share Summary</h2>
                <form action="/share" method="POST" class="space-y-6">
                    <input type="hidden" name="customPrompt" value="<%= customPrompt %>">
                    <div>
                        <label for="summary" class="block text-sm font-medium text-gray-700 mb-1">Generated Summary (Editable)</label>
                        <textarea name="summary" id="summary" rows="15"
                                  class="mt-1 block w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm"><%= summary %></textarea>
                    </div>
                    <div>
                        <label for="recipients" class="block text-sm font-medium text-gray-700 mb-1">Recipient Email Addresses</label>
                        <input type="email" name="recipients" id="recipients" required multiple
                               placeholder="e.g., colleague1@example.com, manager@example.com"
                               class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        <p class="mt-2 text-xs text-gray-500">Separate multiple emails with a comma.</p>
                    </div>
                    <div>
                        <button type="submit"
                                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition-colors">
                            Share via Email
                        </button>
                    </div>
                </form>
            </section>
            <% } %>
        </main>

        <footer class="text-center mt-8 text-sm text-gray-500">
            <p>Powered by Node.js, EJS, and the Google Gemini API.</p>
        </footer>
    </div>

</body>
</html>